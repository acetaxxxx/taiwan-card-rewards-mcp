{
  "name": "taiwan-card-rewards-mcp",
  "version": "0.2.0",
  "type": "module",
  "files": ["dist"],
  "bin": {
    "taiwan-card-rewards-mcp": "dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js",
    "./mcp-contract": "./dist/mcp-contract.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prebuild": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
