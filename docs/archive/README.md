# Archived work

This directory preserves completed or superseded planning material that no
longer defines the current implementation or runtime skill. Current work lives
in `.scratch/`; current product and integration guidance lives elsewhere under
`docs/`.

Archived files are historical evidence. Do not use them to derive MCP tool
names, payloads, installation steps, or runtime skill instructions.
