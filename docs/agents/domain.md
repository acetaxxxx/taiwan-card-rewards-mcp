# Domain Docs

This is a single-context repository.

- Read `CONTEXT.md` at the repo root before exploring domain code.
- Read relevant ADRs under `docs/adr/`.
- Use the glossary vocabulary from `CONTEXT.md` in issue titles, specs, tests, and design notes.
- Keep domain terms in `CONTEXT.md`; record hard-to-reverse architectural trade-offs in `docs/adr/`.
